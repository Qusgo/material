# 多材料物理画布实现计划

## 0. 实现原则

目标不是一次写出完美物理引擎，而是谨慎地把复杂系统拆成可验证的小层：

- 第一优先级：手机上可运行，不爆帧、不爆内存。
- 第二优先级：规则一致，用户能预测后画覆盖、力、橡皮、暂停的行为。
- 第三优先级：水压、沙子、粘土、刚体逐步逼近真实感。
- 所有核心物理先用 CPU + Canvas 2D，不用 GPU。
- 保留单文件 `index.html`，但内部代码要模块化组织。

## 1. 当前目标规格

已确认的目标：

- 白色画布。
- 材料：水、沙子、粘土、石头。
- 密度：水 < 沙子 < 粘土 < 石头。
- 画出来就是材料本身。
- 后画覆盖旧材料。
- 填充只填鼠标所在位置的同类连通区域，避免覆盖全画布。
- 固定石头：任意手绘形状，模拟中不动，可被后续编辑覆盖。
- 动态石头：只能是圆形或矩形刚体。
- 动态矩形：初始水平/竖直拖出，模拟中可旋转。
- 动态刚体可以覆盖水、沙子、粘土、固定石头，但不能覆盖已有动态刚体。
- 力工具：画圈 + 画箭头。
- 力对网格材料：圈内 cell 获得速度。
- 力对动态刚体：只要力圈和刚体相交，整个刚体受力；偏离质心时产生角速度。
- 水需要压强，U 型管液面应趋于平衡。
- 沙子落入水中下沉并挤开水。
- 粘土受力后应发生大概形变。
- 橡皮暂停时间，可擦材料和刚体。

## 2. 总体架构

建议使用三层数据：

1. 网格层  
   管水、沙子、粘土、固定石头、空白。

2. 动态刚体层  
   管圆形石头、矩形石头。

3. 交互/预览层  
   管画笔预览、填充预览、力圈/箭头预览、橡皮预览。

渲染顺序：

1. 白色背景。
2. 网格材料。
3. 动态刚体。
4. 交互预览。
5. UI 状态文字。

模拟顺序：

1. 如果暂停，不更新物理，只响应编辑。
2. 应用待处理编辑操作。
3. 更新动态刚体速度和位置。
4. 刚体和固定石头/边界碰撞。
5. 刚体和网格材料交互。
6. 更新沙子。
7. 更新粘土。
8. 更新水压/水流。
9. 清理离开画布的动态材料。
10. 标记 active cells。

## 3. 网格数据结构

使用固定物理分辨率，而不是直接用屏幕像素。

建议第一版：

- `CELL_SIZE = 4` 或 `5` CSS px。
- 手机上动态按画布大小限制最大网格，例如最多约 `220 x 360`。
- 大屏也不要无限增加物理分辨率，可以让视觉缩放。

TypedArray：

```text
material: Uint8Array
mass: Float32Array
vx: Float32Array
vy: Float32Array
flags: Uint8Array
active: Uint8Array
nextActive: Uint8Array
```

材料枚举：

```text
0 empty
1 water
2 sand
3 clay
4 fixedStone
```

动态石头不放进 `material` 作为主数据，而是放在刚体数组中。渲染和碰撞时再投影到网格或采样。

原因：

- 动态刚体需要连续坐标、角度、角速度。
- 如果把动态石头直接塞进网格，会很难旋转。

## 4. 动态刚体数据结构

```text
body = {
  id,
  type: "circle" | "rect",
  x, y,
  vx, vy,
  angle,
  angularVelocity,
  radius,          // circle
  halfWidth,       // rect
  halfHeight,      // rect
  mass,
  invMass,
  inertia,
  invInertia,
  asleep
}
```

第一版限制：

- 动态刚体数量设上限，例如 64 或 128。
- 线速度限幅。
- 角速度限幅。
- 每帧移动拆成小步，防止穿墙。

矩形碰撞策略：

- 矩形顶点由中心、半宽高、角度计算。
- 和画布边界用顶点检测 + 推出。
- 和固定石头用采样点检测。
- 和其他刚体用圆-圆、圆-矩形、矩形-矩形的近似 SAT。

谨慎建议：

- 第一版矩形-矩形碰撞可以只做到“合理弹开”，不要追求完全真实。
- 矩形和手绘固定石头碰撞先用多个采样点，不做复杂多边形轮廓。

## 5. 绘画与覆盖

工具：

- 画笔。
- 填充。
- 橡皮。
- 力。
- 播放/暂停。
- 清空。

材料选择：

- 水。
- 沙子。
- 粘土。
- 固定石头。
- 动态圆石头。
- 动态矩形石头。

画笔：

- 对水/沙/粘土/固定石头：拖动时写入网格。
- 必须做连续补点，避免快速拖动断线。
- 写入时后画覆盖旧 material。
- 如果覆盖动态刚体，要不要删除该刚体需要谨慎。建议第一版：画笔不覆盖动态刚体，橡皮专门删除动态刚体。

填充：

- 鼠标所在 cell 的材料类型作为 target。
- flood fill 找同类连通区域。
- hover 时显示半透明预览。
- 点击时把该区域改成当前材料。
- 最大填充区域设上限，避免手机卡死。

动态刚体放置：

- 动态圆：按下为圆心，拖动为半径。
- 动态矩形：按下为一个角，拖动为对角，初始角度 0。
- 放置预览实时显示。
- 若和已有动态刚体重叠，预览变红，禁止放置。
- 放置时覆盖网格材料，包括固定石头；覆盖区域直接清空或替换。

这里有一个推荐规则：

- 动态刚体覆盖网格时，删除被覆盖网格材料。
- 模拟过程中动态刚体推动材料时，才尝试挤开材料。

## 6. 水压算法

水用 mass-grid，不用粒子水。

每个水 cell：

- `material = water`
- `mass > 0`

参数：

```text
MAX_MASS = 1.0
MAX_COMPRESS = 0.25
MIN_MASS = 0.005
MAX_FLOW = 0.35
FLOW_DAMPING = 0.5
```

更新方向：

1. 向下流。
2. 向左右流。
3. 受压时向上流。

核心原则：

- 下方可容纳更多 mass 时优先下流。
- 横向根据质量差找平。
- 下方压力过高时允许向上传一点质量。
- 每次 flow 都限幅，防止震荡。

U 型管验证：

- 固定石头画一个 U 型管。
- 左侧填水。
- 播放后水应流到底部，再进入右侧。
- 最终左右液面高度接近。
- 不要求瞬间精确，但不能永远只停在一侧。

## 7. 沙子算法

沙子是 occupied-grid 材料。

规则：

1. 如果下方空，向下。
2. 如果下方是水，和水交换，水被挤开。
3. 如果斜下空，随机向左下/右下。
4. 如果斜下是水，也可交换。
5. 否则停住。

沙子速度：

- 可受力改变 `vx, vy`。
- 但沙子高摩擦，速度快速衰减。
- 大多数时候仍按 cellular automata 下落。

和水交互：

- 沙子进入水格时，沙子占格。
- 水 mass 尝试转移到邻近水/空 cell。
- 找不到位置时，允许少量压缩水。
- 最后才考虑删除极小 mass，避免水凭空大量消失。

## 8. 粘土算法

第一版粘土建议偏“橡皮泥”：

- 成团。
- 受小力时保持形状。
- 受大力时缓慢变形。
- 比沙子更少横向散开。

规则：

1. 下方空且支撑少时，慢速下沉。
2. 外力超过 `yieldThreshold` 后，按速度方向移动。
3. 邻居越多，越不容易移动。
4. 速度衰减强。

近似形变：

- 不是整块刚体。
- 也不是液体。
- 每个 cell 可以移动，但 cohesion 让它倾向保持团块。

## 9. 力工具

交互：

1. 选择力工具。
2. 拖出圆形作用区。
3. 再拖出箭头。
4. 松开后施力。

对网格材料：

- 遍历力圈覆盖的 cell。
- 对水/沙/粘土添加速度。
- 固定石头不受力。

对动态刚体：

- 对每个 body 做 force circle intersection。
- 相交则整块 body 受力。
- 线速度按质量缩放。
- torque 由作用点偏离质心计算。

交集测试：

- 圆 body vs 力圈：圆圆相交。
- 矩形 body vs 力圈：把力圈中心转入矩形局部坐标，做 circle-AABB closest point。

限制：

- 最大力。
- 最大速度。
- 最大角速度。
- 大力也不能让刚体瞬移穿墙。

## 10. 橡皮

橡皮进入时自动暂停。

擦除规则：

- 擦网格材料：把覆盖 cell 设为空。
- 擦动态刚体：如果橡皮圆和 body 相交，删除整个 body。

原因：

- 刚体不能被擦掉一角，否则又变成任意形状刚体。
- 如果以后想“切割刚体”，那是单独功能。

## 11. 性能策略

第一版必须保守：

- 统一网格，不做多尺度颗粒。
- active cells，只更新动过的区域和邻居。
- 水可以分区或隔帧更新。
- 粘土低频更新。
- 动态刚体数量上限。
- flood fill 分帧或限制最大区域。
- 渲染用 ImageData 批量写像素，再 drawImage 放大。

建议帧率：

- 桌面：目标 60 FPS。
- 手机：目标 30 FPS。

## 12. 实现阶段

### Phase 1: 重写基础框架

目标：

- 单文件页面仍能打开。
- 有 Canvas、toolbar、材料选择、暂停/播放。
- 有网格数据结构。
- 能画水、沙、粘土、固定石头。
- 能橡皮擦。

验证：

- 快速拖动画笔不断线。
- 后画覆盖旧材料。
- 橡皮不会留下残影。

### Phase 2: 填充与预览

目标：

- hover 显示同类连通区域。
- 点击填充当前材料。
- 大区域不卡死。

验证：

- 点空白只填同一片空白。
- 点水只替换连通水体。
- 不会一键覆盖全画布。

### Phase 3: 水压

目标：

- mass-grid 水工作。
- U 型管能找平。

验证：

- U 型管左右液面趋于平衡。
- 水不会无限抖动。
- 水不会大量凭空消失。

### Phase 4: 沙子和粘土

目标：

- 沙子下落、堆积、入水下沉。
- 粘土受力形变。

验证：

- 沙子能穿过水下沉。
- 粘土被力推时变形，但不立刻像水一样散开。

### Phase 5: 动态刚体

目标：

- 动态圆。
- 动态矩形。
- 矩形可旋转。
- 不能覆盖已有动态刚体。
- 可覆盖网格材料。

验证：

- 圆/矩形受重力下落。
- 碰到固定石头和画布边界会停/弹/滚。
- 矩形能旋转，但不会疯狂穿墙。

### Phase 6: 力工具

目标：

- 画圈 + 箭头。
- 网格材料局部受力。
- 动态刚体整体受力并产生角速度。

验证：

- 力圈碰到刚体一角，整块刚体移动/旋转。
- 固定石头不受力。
- 大力不会直接把物体打穿墙。

### Phase 7: 打磨和参数

目标：

- 调水压。
- 调沙子坡角。
- 调粘土屈服。
- 调刚体摩擦/弹性。
- 手机性能测试。

## 13. 谨慎实现顺序建议

不要先写完整 UI，再写物理。建议按可验证核心先行：

1. 网格和渲染。
2. 画笔/橡皮。
3. 水压 U 型管。
4. 沙子入水。
5. 粘土形变。
6. 动态圆/矩形刚体。
7. 力工具。
8. 填充预览和大面积优化。

其中水压和刚体是风险最高的两块，应该分别做小 demo 级验证，再合并。

## 14. 仍需确认的小决策

1. 粘土风格：更像橡皮泥，还是更像湿泥块？  
   我建议第一版偏橡皮泥。

2. 第一版是否需要撤销/重做？  
   我建议先不做，避免状态历史占内存；先保留清空和橡皮。

3. 动态刚体数量上限多少？  
   我建议手机默认 64，桌面可调到 128。

4. 网格尺寸是否接受自动降级？  
   我建议根据屏幕和设备像素比自动控制物理分辨率。
