# 多材料 + 力 + 水压模拟设计讨论

## 1. 我对需求的理解

你现在想把原来的“容器 + 水 + 橡皮”升级成一个更通用的 2D 物质模拟器：

- 白色画布仍然是主区域。
- 材料有四种：水、沙子、粘土、石头。
- 水、沙子、粘土默认都受重力并参与运动。
- 石头有两种模式：
  - 固定石头：不受重力，也不受外力，是容器/墙体。
  - 动态石头：受重力，尽量表现得像刚体。
- 所有材料的绘画逻辑一致：
  - 可以多笔画边界/区域。
  - 后画的材料覆盖先画的材料。
  - 鼠标移动到可填充区域时显示预览。
  - 点击该区域才真正填充。
- 加一个“力”工具：
  - 先画一个圈，圈定作用区域。
  - 再画一个箭头。
  - 圈内所有物质获得一个速度。
  - 速度方向由箭头方向决定，速度大小和箭头长度成比例。
- 水需要有一定压强，能在 U 型管里逐渐达到两边液面平衡。
- 尽量保持计算成本低，最好普通手机也能流畅运行。

这个目标已经接近一个轻量版 falling-sand / cellular-automata sandbox，而不是单纯绘图工具。

## 1.1 已确认的关键决策

根据进一步讨论，先把几个会影响整体架构的点固定下来：

- 画出来的线就是材料本身，不再是单纯的填充边界。
- 水、沙子、粘土默认都受重力。
- 只有石头可以是不受重力的静态材料。
- 不受重力的石头在物理模拟中不受外力影响，等价于固定刚体/墙体。
- 但在用户编辑层面，后续画上去的任何材料都可以覆盖固定石头。
- 固定石头可以是任意手绘形状。
- 动态石头不再是任意手绘形状，而是用户生成的简单刚体：圆或可旋转矩形。
- 不同动态石头刚体之间不能互相连接，也不能互相覆盖。
- 动态刚体放置时可以覆盖水、沙子、粘土、固定石头；但不能覆盖已有动态石头。
- U 型管水压平衡最好第一版就作为核心目标来做。
- 力工具可以影响受重力石头，但因为密度最大，响应应该最弱。
- 四种材料密度从小到大：水 < 沙子 < 粘土 < 石头。
- 沙子落进水里应该下沉，并把水挤开。
- 粘土可以和水有交互，但第一重点是受力时产生大概的形变。
- 填充规则采用“同类连通区域”：鼠标在哪种旧材料或空白上，就只预览/填充与它连通的同类区域，避免一键覆盖全画布。

这些决定意味着：固定石头是主要容器/障碍材料；动态石头是独立圆形或矩形刚体；水需要从一开始就用能表达压强的模型；沙子和粘土是可动颗粒/半颗粒材料；力工具作用于水、沙子、粘土和动态石头，不作用于固定石头。

## 2. 总体建议：用网格元胞模拟，不用真实流体

我建议继续使用 Canvas 2D，但底层改成固定网格模拟：

- 画布被划分成小格子，比如每格 4px 或 5px。
- 每个格子最多存一种材料。
- 每个格子记录：
  - material：empty / water / sand / clay / stone
  - gravity：true / false
  - vx, vy：速度，用于力工具和惯性
  - age 或 order：用于“后来的在上面”
  - extra：材料需要的附加状态，比如水的 mass

好处：

- CPU 成本可控。
- 手机上也更稳。
- 材料之间交互简单明确。
- 橡皮、填充、力圈都很容易映射到格子。
- U 型管水压可以用近似压强算法实现，不需要 WebGL 或 GPU。

代价：

- 视觉会有像素/颗粒感。
- 水不是严格 Navier-Stokes 流体。
- 粘土、沙子、石头需要用规则近似真实行为。

我觉得这个取舍是对的，因为你的目标是“交互丰富 + 手机可跑”，不是科研级流体。

## 3. 四种材料的物理建议

我不建议只用“需要多少力才开始流动”这一个参数。更好的抽象是几个材料参数组合：

- density：密度，决定沉浮和推动关系。
- yieldThreshold：屈服阈值，外力超过多少才明显运动。
- viscosity：粘滞程度，决定速度衰减和扩散慢不慢。
- friction：摩擦/堆积倾向，尤其影响沙子和石头。
- cohesion：粘结程度，影响粘土是否成团。
- gravityEnabled：是否受重力。
- mobile：是否可被力推动。

建议初始材料行为：

| 材料 | 行为建议 |
| --- | --- |
| 水 | 低屈服、低摩擦、低粘度，会流动，会横向找平，有压强 |
| 沙子 | 密度高于水，受重力时向下掉，遇阻向左右斜下滑；落入水中会下沉并挤开水 |
| 粘土 | 密度高于沙子，高粘度、高屈服，受力后慢慢形变，容易成团 |
| 石头 | 密度最高；固定时作为任意形状刚性墙；动态时作为独立圆形或矩形刚体，受力响应最弱 |

石头的难点不是“刚体”两个字本身，而是“手绘任意形状刚体 + 与网格流体/颗粒耦合”。像 Angry Birds 这类游戏通常是少量预制多边形/圆形刚体，交给成熟物理引擎做碰撞和旋转。

新的方案把石头拆成两类，这很合理：

- 固定石头：任意手绘形状，只作为静态障碍参与碰撞和水压边界。
- 动态石头：只能是圆形或矩形刚体，不允许彼此连接或覆盖。

这样动态石头就可以用简单刚体处理：位置、速度、角度、角速度、质量、转动惯量、碰撞法线都比较明确。圆形最简单；矩形旋转的计算量也可以接受，关键是限制动态刚体数量并做好分步碰撞。它仍然可以和固定石头、水、沙子、粘土交互，但不会出现任意手绘刚体的轮廓分解问题。

## 4. 重力开关的交互建议

现在确认只有石头需要固定/受重力两种状态，所以 UI 可以更简单：

- 一个材料选择：水 / 沙 / 粘土 / 石头。
- 当材料是石头时，显示一个模式切换：固定 / 动态圆 / 动态矩形。
- 水、沙子、粘土不显示重力开关，默认都会动。

这样能避免 8 个材料按钮造成混乱，也符合“固定材料本质上就是石头墙体”的设定。

动态石头的交互建议：

- 动态圆：按下确定圆心，拖动确定半径，松开放置。
- 动态矩形：拖出水平/竖直初始矩形，初始角度为 0；放置后作为可旋转刚体参与模拟。
- 如果新刚体会和已有动态石头刚体重叠，则显示红色预览并禁止放置。
- 动态刚体可以覆盖水、沙子、粘土、固定石头；被覆盖区域按编辑操作直接删除旧材料。
- 动态刚体之间不覆盖，也不合并。

## 5. 绘画和填充逻辑建议

现在确认：画出来的线就是材料本身。

所以我建议绘画系统改成两个互补动作：

- 画笔：拖动时直接把当前材料写入网格。
- 填充：鼠标/手指移动到可填充区域时显示预览，点击后把该区域填成当前材料。

这两个动作使用同一种材料设置：

- 当前材料：水 / 沙子 / 粘土 / 石头。
- 当前石头模式：固定 / 受重力。
- 当前笔刷大小。

填充区域目前按你的决定，边界只认画布边缘；固定石头也可以被后续材料覆盖，所以不能把固定石头当作不可覆盖的填充边界。流程可以是：

1. 用户选择材料。
2. 用户选择画笔或填充动作。
3. 鼠标移动到可填区域时，系统显示半透明预览。
4. 点击后填充当前材料，后画材料覆盖旧材料。

这样可以同时满足“画线就是材料”和“点击区域填充”：

- 想画细长物质：直接拖动画笔。
- 想填满区域：用 hover 预览 + 点击填充。

潜在问题：

- 如果填充边界真的只有画布边缘，那么在普通 flood fill 语义下，点击任何位置都会预览/填充整个画布。
- 如果你想要“鼠标移动到某个局部可填充区域时显示”，还需要一个额外规则来定义局部区域，比如只填充同一种旧材料、只填充空白区域、或用当前画笔画出的选区轮廓。
- 后画覆盖一切是清楚的；真正需要定的是“填充预览区域如何被分割出来”。

## 6. “后来的在上面”的规则

网格里一个格子只能有一种材料时，“后来的在上面”最简单：

- 新填充的材料直接覆盖旧材料。

如果未来想做更真实的叠层，比如水覆盖在石头上但不替换石头，就会复杂很多。手机友好版本建议先坚持单格单材料。

材料优先级可以这样：

- 固定石头/固定材料：模拟中不被动态材料挤掉，但用户后续绘画/填充可以覆盖。
- 用户新画/新填充：总是最高优先级，可以覆盖任何旧材料。
- 模拟过程中：材料按密度和规则交换位置，比如沙子可以穿过水下沉，水会上浮或被挤开。

## 7. 力工具建议

力工具可以设计成两步：

1. 画圈：定义作用区域。
2. 画箭头：定义方向和强度。

对水、沙子、粘土这类网格材料，圈内每个可移动格子的速度增加：

```text
cell.vx += arrow.dx * forceScale * materialResponse
cell.vy += arrow.dy * forceScale * materialResponse
```

对动态石头刚体，规则不同：只要力圈和刚体形状有交集，就给整个刚体加速度，而不是只影响圈内那几个格子：

```text
body.vx += arrow.dx * forceScale * bodyResponse / body.mass
body.vy += arrow.dy * forceScale * bodyResponse / body.mass
```

因为矩形需要旋转，力的作用点可以用“力圈中心到刚体质心的偏移”估算 torque：

```text
body.angularVelocity += cross(forcePoint - body.center, force) / body.inertia
```

圆形也可以记录角速度；圆的旋转主要是视觉效果，但碰撞摩擦时会更自然。

不同材料对力的响应不同：

- 水：响应强，速度衰减中等。
- 沙子：响应中等，但会被摩擦快速削弱。
- 粘土：响应弱，除非箭头很长超过屈服阈值。
- 动态石头：响应很弱；只要和力圈相交就整体响应。固定石头不响应。

建议加两个限制：

- 最大速度上限，避免一箭头把东西打穿墙。
- 每帧最多移动若干格，避免高速粒子越过容器。
- 动态石头刚体要做连续或分步碰撞，避免高速时穿过薄墙。

潜在问题：

- 力圈如果很大，一次性给所有格子加速度可能卡顿。
- 可以只作用于圈内非空格子，并分批处理，或者限制最大圈面积。
- 对刚体不能按粒子局部受力，否则一个矩形被圈住一角时会被“撕开”。刚体必须整体移动。

## 8. 水压和 U 型管平衡

这是最关键也最难的部分。

普通 falling-sand 水算法只会“往下、往左右流”，通常不能很好地在 U 型管里平衡，因为它缺少向上传递压强的能力。

我建议使用“可压缩水质量”模型：

- 每个水格不只是 occupied / empty，而是有 mass。
- 正常一格水 mass 接近 1。
- 下面压力大时，水可以把质量传到旁边甚至上方。
- 用 MaxMass 和 MaxCompress 控制压缩程度。

大致行为：

- 水优先往下流。
- 下方满了以后向左右流。
- 如果局部压力高，允许少量向上/侧向传递。
- 连通水体会逐渐找平，因此 U 型管两边液面能趋于一致。

这是一个经典低成本近似，适合手机。

建议不要一开始做粒子式水，而是做 mass-grid 水：

- 水格可以是 0.0 到 1.0+ 的质量。
- 渲染时根据质量深浅显示蓝色。
- 这样 U 型管找平比离散小粒子更自然。

潜在代价：

- 水和沙子、粘土、石头交互时需要定义交换规则。
- 水质量模型和其他材料的“单格材料”模型要统一设计。

一个折中方案：

- 水用 mass-grid。
- 沙子/粘土/石头用 occupied-grid。
- 一个格子如果是非水固体，就不能存水。
- 沙子落入水中时，沙子占格，水被挤到邻近可用水格。

## 9. 粘土的建议实现

粘土容易变成性能和规则黑洞。

建议第一版粘土这样做：

- 像非常粘的沙子。
- 受重力时会慢慢下沉。
- 只有当下方空、或者受到足够力时才移动。
- 横向移动概率很低。
- 成块移动先不做，避免复杂刚体/软体模拟。

以后如果想更像真实粘土，再加：

- cohesion：和邻居连接越多，越不容易动。
- shear：受到侧向力时逐步拉伸变形。

## 10. 手机性能预算

建议的性能目标：

- 内部网格：最多约 160 x 240 到 220 x 320。
- 每帧只更新 active cells，不扫全图。
- 目标帧率：30 FPS。
- 模拟步长：每帧 1 到 2 次。
- 大面积填充、力圈、重建连通区域可以分批做，避免单帧卡顿。

关键优化：

- active set：只更新最近动过的格子和它周围邻居。
- dirty rect：只重绘变化区域，或者 Canvas 仍全量绘制但模拟不全量扫。
- typed arrays：材料、质量、速度都用 TypedArray。
- 固定分辨率模拟：视觉画布可以高清，物理网格保持低分辨率。
- 限制最大填充区域和最大力圈半径。

如果想优先保证手机体验，我建议物理格子不要小于 4px。

### 10.1 不同材料颗粒尺寸的取舍

“沙颗粒比水大、粘土颗粒比沙子大”可以减少粒子数，但会带来一个额外复杂度：不同尺寸材料之间要做多尺度碰撞和置换。例如一个沙粒占 2x2 个水格时，沙子下沉需要一次挤开多个水格；粘土更大时还要处理局部形变和断裂。

我的建议：

- 第一版物理层保持同一网格分辨率，水、沙子、粘土、石头都占用同一套 cell。
- 水用 `mass` 表达连续性和压强，不靠更小粒子。
- 沙子仍是一格一粒，但只更新 active cells，性能通常可以接受。
- 粘土可以用“团块/连通块倾向”减少运动频率，而不是真的放大粒子尺寸。
- 视觉上可以把沙子画得比水粗、粘土画得更成块，但底层先不要多尺度。

如果后面性能不够，再考虑第二版优化：

- 沙子使用 2x2 cluster。
- 粘土使用更大的 deformable cluster。
- 水保持最细网格。

这会更省算力，但材料交互规则也会明显更复杂。

## 10.2 需要提前防的潜在 bug

1. 动态石头穿墙  
   如果刚体速度太大，每帧只检测当前位置，圆或矩形可能直接跳过固定石头薄墙。解决方案是限制最大速度，并把一次移动拆成多个小步做碰撞。

2. 旋转矩形卡进固定石头  
   旋转矩形碰到凹凸不平的手绘固定石头时容易角点卡住。需要做“推出”逻辑：检测重叠后沿最小位移方向推出，失败时减速并削弱角速度。

3. 刚体和水压互相爆炸  
   如果动态石头压进水里，一次性挤开的水太多，水可能被强行压缩到不稳定。需要给水的最大质量、流量、压力传递速度设上限。

4. 动态刚体覆盖旧材料时质量不守恒  
   放置动态石头如果覆盖水/沙/粘土，被覆盖材料是删除、挤开，还是转换为飞散粒子，需要定规则。最简单是编辑操作直接覆盖删除；模拟中的碰撞才挤开。

5. 力圈和刚体交集判断不一致  
   圆和矩形都要有明确 intersection 测试。圆-力圈简单；旋转矩形-力圈需要把力圈中心变换到矩形局部坐标，再检查圆到 AABB 的最近点距离，不能只看矩形中心是否在圈内。

6. 同类连通填充太大导致卡顿  
   在手机上点到大片空白时，连通区域可能接近全画布。需要预览区域缓存、最大填充上限，或者分帧 flood fill。

7. 后画覆盖固定石头导致容器突然漏水  
   这是符合规则的，但用户可能误触。可以在 UI 上把固定石头颜色/图层感做清楚，或者让橡皮和覆盖都有明显预览。

8. 水在 U 型管里震荡不收敛  
   mass-grid 水如果压力传递太强，会来回震荡；太弱则找平太慢。需要 damping 和最大流量限制。

9. 沙子置换水时产生空洞  
   沙子下沉挤水时，如果附近没有可接收水格，直接删除水会不自然，强行压缩水又可能爆。需要优先侧向/向上找空位，找不到再允许少量压缩。

10. 粘土形变和刚体推挤冲突  
    粘土如果太像固体，动态石头推不动；太像液体，又不像粘土。需要屈服阈值：小力保持形状，大力才局部流动。

11. 旋转刚体角速度过大导致隧穿  
    矩形高速旋转时，角点可能扫过薄墙而不被检测到。需要限制角速度，或者把旋转也拆成小步。

12. 动态刚体数量过多导致碰撞成本上升  
    圆/矩形刚体本身不贵，但两两碰撞是 O(n²)。需要限制动态刚体数量，或用网格 broad phase 只检测邻近刚体。

## 11. 我建议的实现阶段

第一阶段：重构数据结构

- 把当前粒子水改为网格材料系统。
- 支持材料选择、石头固定/受重力模式、覆盖规则。
- 支持画笔直接生成材料。
- 支持 hover 填充预览和点击填充。

第二阶段：基础材料行为

- 水：直接做 mass-grid 水，优先验证 U 型管平衡。
- 沙子：下落和堆积。
- 石头：固定任意墙体，以及动态圆/可旋转矩形刚体。
- 粘土：慢速高摩擦颗粒。

第三阶段：力工具

- 圈选区域。
- 箭头施力。
- 材料按响应参数获得速度。

第四阶段：水压打磨

- 调 U 型管找平速度。
- 调水的视觉连续性。
- 处理水和沙子/粘土/石头的挤压与交换。

第五阶段：性能打磨

- active cells。
- 最大区域限制。
- 手机触控体验。
- 参数面板或预设。

## 11.1 审核后的当前规格

这是目前应该按准的版本：

- 画布：白色背景，Canvas 2D，手机优先。
- 材料：水、沙子、粘土、石头。
- 密度：水 < 沙子 < 粘土 < 石头。
- 绘画：画出来就是材料本身；后画覆盖旧材料。
- 填充：只填鼠标所在位置的同类连通区域，避免覆盖全画布。
- 固定石头：任意手绘形状；模拟中不动；可被后续编辑覆盖。
- 动态石头：只能是圆形或矩形刚体。
- 动态矩形：初始只能水平/竖直放置；模拟中支持旋转；不是任意手绘形状。
- 动态刚体覆盖：可以覆盖水、沙子、粘土、固定石头；不能覆盖已有动态刚体。
- 动态刚体连接：不同动态刚体不能连接，也不合并。
- 力工具：先画圈，再画箭头。
- 力对网格材料：圈内格子按箭头获得速度。
- 力对动态刚体：只要力圈和刚体有交集，整个刚体获得线速度；偏离质心时产生角速度。
- 固定石头不受力。
- 水：必须有压强，U 型管液面应能趋于平衡。
- 沙子：会下沉，落入水中挤开水。
- 粘土：受力后要发生大概形变，倾向高粘度/高屈服。
- 橡皮：暂停时间，可擦材料和刚体。
- 性能：统一底层网格，优先 active cells、限速、分步碰撞；暂不做多尺度粒子。

## 12. 仍需要确认的问题

1. 画笔和填充都要有笔刷大小吗？  
   画笔大小比较明确；填充通常没有大小，但可以加“只填局部同类区域”。

2. 粘土受力形变要偏“橡皮泥”还是偏“湿泥块”？  
   橡皮泥更成团、慢慢变形；湿泥块更容易塌陷和铺开。

3. 画笔生成材料时，要不要有“连续线补点”？  
   我建议要做。否则手机手指移动快时会断线，容器漏水。

4. 是否需要撤销/重做？  
   复杂编辑里很有用，但会增加状态保存成本。可以先做清空和橡皮，之后再加撤销。

5. 目标手机规模大概是多少？  
   如果是普通手机浏览器，我建议物理网格控制在约 160 x 260 到 220 x 360 之间。

## 13. 我的推荐方案

我建议下一版不要在当前代码上小修小补，而是做一次结构性升级：

- 保留单文件 `index.html`。
- 把物理核心改成网格模拟。
- 水从一开始就用 mass-grid，这样 U 型管目标不需要返工。
- 沙子、粘土使用材料格子和速度。
- 石头支持固定任意墙体和动态圆/可旋转矩形刚体。
- 绘画交互改成“画笔直接生成材料 + hover 预览 + 点击填充”。
- 力工具作为独立模式加入。

如果你认可这个方向，我下一步可以先写一个更具体的技术设计：

- 数据结构字段。
- 每种材料的更新规则。
- 水压算法伪代码。
- UI 状态机。
- 第一版要实现和暂缓实现的边界。
